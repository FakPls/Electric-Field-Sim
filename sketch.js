let testPoints = [];


function setup() {
  createCanvas(400, 400);
  for(let i = 0; i < 10; i++) {
    for(let j = 0; j < 10; j++) {
      testPoints[i] = create
    }
  }
}

function draw() {
  background(220);
  testPoint.draw();
}

